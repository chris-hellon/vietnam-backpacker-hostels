@model VietnamBackpackerHostels.UI.Pages.Shared.MultiCarouselItemPartialModel

@if (Model.AltText != null)
{
    <img src="@Model.ImageSrc" data-mdb-img="@Model.ImageSrc" class="w-100" alt="@Model.AltText" />
}
else
{
    <img src="@Model.ImageSrc" data-mdb-img="@Model.ImageSrc" class="w-100" />
}